{
  "name": "webdev-test",
  "source": "src/index.html",
  "browserslist": "defaults",
  "scripts": {
    "start": "parcel serve",
    "build": "parcel build",
    "clean": "rm -r dist/ .parcel-cache/"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.22.0",
      "onFail": "download"
    }
  },
  "devDependencies": {
    "parcel": "^2.16.4"
  },
  "dependencies": {
    "inter-ui": "^4.1.1"
  }
}
